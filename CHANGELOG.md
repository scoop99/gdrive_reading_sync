# 변경 이력

이 플러그인의 버전은 `VERSION` 파일의 `plugin version` 이며, BookOasis 본체의
업데이트 버튼과 플러그인 게시판이 그 값을 비교해 갱신 여부를 판단합니다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 를 따르되,
각 항목에 **왜 그렇게 했는지**를 남깁니다 — 나중에 되돌릴지 판단할 때 그게 필요합니다.

---

## [0.4.1] — 2026-09-20

### 고침
- **정리 대기 화면만 보고는 격리/보존을 판단할 수 없던 문제.** v0.4.0 은 돌았지만
  B 등급이 "이름이 비슷한 파일이 있다" 고만 기록하고 **그 파일이 무엇인지 저장하지
  않아**, 사용자가 폴더를 직접 열어보기 전에는 판단할 수 없었습니다.

  이제 B/C 행에 **비교 대상(새 파일)** 을 기록합니다 — 경로·크기·유사 근거. 1순위는
  같은 `NN권` 토큰을 우선하고(`vol:01`), 없으면 `#164` 토큰, 그다음 이름 유사도,
  크기 차이 순으로 고릅니다. **다른 고아 파일은 1순위로 쓰지 않습니다**(`sibling_only`).

- **옛것 vs 새것이 나란히 안 보임 / 쓸모없는 열로 가로 스크롤.** 표를 7열로 다시 짰습니다:
  `등급 · 옛 파일 · 옛 크기 · 새 파일 · 새 크기 · 차이`. `id`·`상태`(기본)·`경로`·`md5`·
  `생성` 을 빼고 **크기 차이**(`-3.0 MB`)를 넣었습니다. 경로는 폴더 헤더로 옮기고
  `table-layout: fixed` + `overflow-x: hidden` 으로 가로 스크롤을 없앴습니다.

- **폴더 단위 묶기.** 128건이 시리즈별로 흩어져 판단 대상이 과대해 보였습니다. 이제
  **폴더로 접혀** 보이고(26개), 펼치면 그 폴더의 옛/새 대응이 한 화면에 나오며,
  `이 폴더 격리` / `이 폴더 보존` 으로 **폴더 단위 일괄 처리**가 됩니다. 폴더에 고아가
  아닌 파일(`07권`, `kavita.yaml`)도 이름·크기로 보여 줍니다.

- **`격리`·`보존` 이 무슨 뜻인지 없던 문제.** 버튼 아래에 보이는 문구로 설명합니다 —
  격리 = **삭제 아님**, `_trash/날짜/원래경로` 로 이동, 30일 안에 되돌림. 보존 = 파일은
  그대로 두고 다시 묻지 않음.

### 참고
- 기존 pending 128건은 **기동 시 1회 재분류**로 후보가 채워집니다 (백필 가드와 무관,
  멱등 UPDATE). 늦은 create 로 C/B 가 A 로 올라가는 동작은 그대로입니다.
- **A/B/C 판정 규칙은 바뀌지 않았습니다.** 후보는 기록·표시용입니다.
- 성능: 이미 md5(32자)+크기가 맞는 고아는 **재해시하지 않습니다**. size 다른 이웃은
  해시하지 않습니다(A 게이트 유지).
- 목록 API 에 `group=folder` 를 추가했습니다(기본은 기존 평평한 응답). **영구삭제
  라우트는 없습니다.** 회귀 116 → **131종**.

---

## [0.4.0] — 2026-09-20

### 추가
- **고아 파일 격리(정리 대기).** 구드에서 파일 **이름만 바꾸면** 파일 ID 가 달라
  `delete` + `create` 로 들어와, 로컬에 **옛 파일과 새 파일이 둘 다** 남고 BookOasis 가
  **책을 두 권**으로 등록하던 문제를 고칩니다.

  이제 원격 삭제를 받은 로컬 파일은 **고아 큐**에 오르고, 분류 결과에 따라 처리됩니다.

  | 등급 | 조건 | 처리 |
  |---|---|---|
  | **A** | 같은 폴더에 **md5 가 같은** 다른 이름 파일이 있음 | **자동**으로 `_trash` 로 이동 |
  | **B** | md5 는 다르나 이름이 유사(`#164` 토큰 공유 또는 유사도 ≥ 0.55) | 정리 대기에 남김 → 사람이 선택 |
  | **C** | 대체 파일 없음 | 정리 대기에 남김 → 기본 보존 |

  **파일을 영구 삭제하지 않습니다.** `L:\READING\_trash\YYYY-MM-DD\<원래 상대경로>` 로
  **이동**(`os.replace`)만 합니다. 원래 상대경로를 보존해 동명 파일 충돌을 피하고,
  `_trash` 에 `.bookoasisignore`(`*`)를 심어 본체 스캐너가 휴지통을 읽지 않게 합니다.
  `_trash` 는 `RETENTION_DAYS`(기본 30일)가 지난 **날짜 폴더만** 정리합니다(자동 정리와
  같은 1시간 게이트). 영구 삭제는 이 한 곳뿐입니다.

  격리 후 원래 책 폴더를 증분 스캔에 넣어, 본체 DB 의 옛 권도 정리되게 합니다.

### 고침
- **`보관함 없음` 오탐 (v0.3.4 부터).** BookOasis 보관함은 `physical_path` 에 경로를
  **여러 줄**로 넣을 수 있는데(`id=40` 웹소설 등), 플러그인이 이를 경로 한 개로 취급해
  존재하지 않는 경로와 비교했습니다. 이제 줄 단위로 쪼개 매칭합니다 (경로 50 → 63개).
  본체 스캐너와 같은 split 규칙을 씁니다.

### 참고
- **삭제 판정을 D1 보다 앞으로 옮겼습니다** (`sync_worker._prepare_copy_job`). 삭제 job 에
  캐시 md5 를 실으면, 그대로는 D1 중복 판정에 삼켜져 격리 큐에 안 들어갑니다.
- 고아 상태는 `job` 컬럼이 아니라 **별도 `orphan` 테이블**에 둡니다. `cleanup_terminal` 이
  `skipped(remote_deleted)` 이력을 30일 뒤 지우므로 job 에 붙이면 큐가 증발합니다.
- 과거 `remote_deleted` job 은 기동 시 **1회** 백필합니다 (`orphan_backfill_done` 가드).
- API: `GET /orphans`, `POST /orphans/isolate`, `POST /orphans/keep`. **영구삭제 라우트 없음.**
- 화면: `변경 이벤트` / `정리 대기` 하위 탭.
- 회귀 96 → **116종** (`T-P13-NODEL` 이 영구삭제/이동 정적 검사를 보증).

---

## [0.3.11] — 2026-09-14

### 변경
- **아무 일도 없는 폴링 로그를 접었습니다.** 초기 대량 동기화가 끝나면 대부분의
  폴링 사이클은 "변경 없음" 인데, 그것까지 매번 기록하면 정작 의미 있는 줄이
  0 사이에 묻힙니다(실측: 하루 1,176줄 중 1,097줄, 93.7%가 전부 0).

  이제 **실제로 뭔가 일어난 사이클만** 기존 형식으로 남기고, 조용한 사이클은
  **한 시간에 한 줄로 접어** 남깁니다:

  ```
  poll idle 55 cycles changes=120 (전부 대상 밖: out_of_root=118 excluded=0
            ext_skip=2 probe_skip=0) token=8309640
  ```

  실측 기준 로그 줄이 **90~98% 줄어듭니다**(9/14: 1,176줄 → 약 27줄).

### 참고
- **폴링 생존 확인은 그대로 됩니다.** 접힌 줄에 `token` 을 함께 적어, 한 줄로
  "살아 있음 + Drive 변경을 계속 받는 중" 이 동시에 확인됩니다. 조용해진 직후
  첫 한 줄은 즉시 남기므로 생존 증거가 비는 구간이 없습니다.
- **사건은 절대 접지 않습니다.** 작업 생성 · 폴더 이벤트 · 무변경 억제 · 경로 해석
  오류 · 폴링 실패가 있으면 기존 형식 그대로 기록합니다. 밀려 있던 조용한 사이클은
  그 **앞에** 요약으로 먼저 남겨, 언제부터 조용했다가 언제 일이 생겼는지 읽힙니다.
- 회귀 2종 추가(총 96종): 조용하면 접히는지 · 사건과 오류는 반드시 남는지.

---

## [0.3.10] — 2026-09-11

### 고침
- **플러그인 게시판 설치가 막히던 문제.** 게시판이
  `플러그인 검증 실패 — 필수 필드: 클래스에 없음: name` 으로 설치를 거부했습니다.

  원인: 게시판은 소스를 **AST 로 정적 검사**하며, `name` 이 **리터럴 문자열**일 때만
  필수 필드로 인정합니다(`plugin_board.py:1641`). 0.3.5 에서 버전을 붙이려고
  `name = "구드 독서 동기화" + _VER_SUFFIX` 로 바꾼 것이 `ast.Constant` 가 아니어서
  인식되지 않았습니다. 런타임에는 정상 동작하므로 설치를 시도하기 전까지 드러나지 않습니다.

  `name` 을 리터럴로 되돌렸습니다. **화면 표시는 달라지지 않습니다** — 페이지 제목의
  버전은 `/status` 의 `plugin_version` 을 읽어 붙이는 별도 경로이며 그대로입니다.
  플러그인 관리 목록의 이름에서만 버전이 빠집니다.

### 참고
- 같은 함정에 두 번 걸렸습니다(0.3.2 `config_schema`, 0.3.5 `name`). 재발을 막으려고
  게시판 검증 로직을 그대로 재현한 회귀 테스트를 넣었습니다
  (`test_sync_worker.py::test_T_BOARD_required_fields_are_ast_literals`).
  `id`/`name`/`is_searchable`/`config_schema`/`category_tab`/`update_manifest` 가
  모두 게시판이 요구하는 리터럴 형태인지 AST 로 검사합니다. 총 94종.
- **f-string, `+` 연결, `%` 포맷, 변수 참조는 전부 안 됩니다. 리터럴만 됩니다.**

---

## [0.3.9] — 2026-09-11

### 추가
- **설정 화면의 경로 칸에 `찾아보기` 버튼.** 이제 경로를 타이핑하지 않고 클릭으로
  고를 수 있습니다. 대상: `로컬 루트`(`LOCAL_ROOT`), `임시 폴더`(`TMP_ROOT`),
  `로그 디렉터리`(`LOG_DIR`), `rclone 실행 파일`(`RCLONE_BIN`),
  `rclone 설정 파일`(`RCLONE_CONFIG`) 다섯 칸.

  **탐색기는 폴더만 선택됩니다.** BookOasis 경로 탐색 API
  (`/api/media/browse-paths`)가 **디렉터리만 돌려주기** 때문이며(본체
  `api/routes/browse_routes.py:126`), 본체 코드는 플러그인이 고칠 수 없습니다.
  파일명이 정해진 두 칸(`RCLONE_BIN`/`RCLONE_CONFIG`)은 **폴더를 고르면 파일명이
  자동으로 붙습니다** — `rclone.exe`(Windows) / `rclone`(그 외), `rclone.conf`.
  다른 파일명을 쓰면 직접 입력하세요.

  이 사실을 화면에 **눈에 보이는 텍스트로** 안내합니다(툴팁 아님) — 두 파일칸의
  도움말, 탐색기 상단 안내줄, 그리고 확정 전 미리보기(`선택하면 입력칸에 들어갈 값: …`).

### 참고
- 권한 검사·허용 루트 제한·경로 탈출 방지는 **본체가 이미 하는 것을 그대로** 씁니다
  (`BROWSE_ALLOWED_ROOTS`, `@admin_required`). 플러그인은 UI 만 만듭니다.
- 본체 탐색기(`window.openPathBrowser`)는 재사용하지 않았습니다 — 결과를
  `library-form-path` 에 하드코딩하고(`path_browser.js:293`), 여러 경로를 누적하며,
  모달 DOM 이 보관함 모달 템플릿에만 있습니다.
- `resolvePickedPath()` 순수 함수 + `test_settings_js.js` 9케이스 추가(총 16케이스).
- 탐색을 취소(취소·✕·배경 클릭·ESC)하면 기존 입력값은 그대로입니다.

---

## [0.3.8] — 2026-09-10

### 변경
- **진행 상황 표시를 실제로 읽을 수 있게 개편.** 진행률 막대 + 퍼센트 + **남은 건수**,
  그 아래 복사·중복·처리중·재시도·**실패**를 각각 분리해 표시합니다.

  이전에는 `전체 404410건 · 완료 4053 · 34.6 GB` 였는데 세 값이 모두 오해를 불렀습니다.
  `전체` 는 감지 이력 총계라 할 일처럼 보이고, `완료` 는 `status='completed'` 만 세서
  **중복으로 끝난 33만 건이 진행에 반영되지 않아 84% 진행이 1% 처럼** 보였습니다.
  남은 건수는 화면 어디에도 없었습니다.

  진행률은 이제 `종결(완료+중복+실패) / 전체` 입니다. 실패 건수는 붉게 표시됩니다 —
  이전 화면에는 실패가 **어디에도 나타나지 않았습니다.**

### 참고
- 백엔드 변경 없음. `status_counts` 가 이미 같은 응답에 실려 오는데 화면이 쓰지 않았습니다.
- `computeProgress()` 를 순수 함수로 분리하고 `test_script_js.js` 추가
  (`node test_script_js.js`).

---

## [0.3.7] — 2026-09-10

### 고침
- **설정을 저장하면 rclone 리모트 설정이 지워지던 문제.** 설정 화면을 열고
  아무것도 건드리지 않은 채 저장만 눌러도 `TRANSFER_REMOTE`·`DETECT_REMOTE` 가
  빈 문자열로 덮어써져 동기화가 멈췄습니다.

  원인: BookOasis 는 플러그인의 `settings.html` 을 파일 그대로 서빙하므로
  `<select>` 에 `<option>` 이 없고, 본체가 `el.value = 설정값` 으로 값을 넣어도
  브라우저가 그 값을 버립니다. 그 상태로 저장하면 빈 값이 기록됩니다.

  이제 저장된 값으로 옵션을 먼저 만들어 복원한 뒤, 실제 리모트 목록을 조용히
  덧붙입니다. **rclone 조회가 실패해도 저장값은 남습니다.**

- **저장된 리모트가 목록에 없을 때 다른 리모트로 조용히 바뀌던 문제.**
  이전 코드는 목록의 첫 항목으로 갈아치웠습니다. 이제 `(설정값 — 목록에 없음)` 으로
  표시하고 선택은 유지합니다.

- **리모트 목록이 처음 한 번만 조회되던 문제.** `RCLONE_BIN`/`RCLONE_CONFIG` 가
  모두 채워진 뒤로는 조회하지 않아, 첫 저장 이후 셀렉트가 영영 비어 있었습니다.
  이제 설정 화면에 들어갈 때마다 조용히 채웁니다.

### 변경
- 사이드 메뉴(카테고리 탭)에서 버전 표기를 제거했습니다. 페이지 제목과
  플러그인 관리 목록의 이름에는 그대로 남습니다.

### 참고
- `pickRemoteOptions()` 순수 함수 + `test_settings_js.js` (`node test_settings_js.js`).

---

## [0.3.6] — 2026-09-10

### 고침
- **내용이 바뀌지 않은 파일에도 `edit` 작업이 생기던 문제.** Google Drive 는 내용과
  무관한 조작(공유·별표·부모 변경 등)에도 변경 이벤트를 발행하는데, 같은 경로면
  무조건 `edit` 작업을 만들었습니다.

  그 작업의 결말은 결국 "중복" 판정인데, 그 판정을 하려고 **로컬 파일 전체를 읽어
  md5 를 계산**합니다. 실측으로 `edit` 76,145건 중 **76,008건(99.8%)이 중복으로
  끝났고, 그 때문에 1,619 GB 를 불필요하게 다시 읽었습니다.**

  이제 원격의 크기와 md5 가 이전과 같으면 작업을 만들지 않습니다.

### 참고
- 이 수정은 상위 프로젝트 FF `bookoasis_mate` 의 `dfa12c0`(v1.10.0) 와 같은 취지이나,
  **판정 기준이 다릅니다.** 상위는 시그니처에 `modifiedTime` 을 포함하는데, 이 플러그인은
  작업 키에 이미 `modified_time` 이 들어가 매번 다른 작업으로 쪼개지므로 그 방식으로는
  한 건도 억제되지 않습니다. 그래서 **크기 + md5 만** 봅니다.
- 폴링 로그에 `unchanged_skip=` 카운터가 추가됩니다. 억제가 실제로 도는지 확인용입니다.
- `store.get_item()` 이 `size`/`md5` 도 반환하도록 확장했습니다(순수 추가). 저장은 하고
  있었는데 읽는 쪽이 빠뜨려, 이 기능이 조용히 무력화될 뻔했습니다.
- **알려진 대가**: 원격이 그대로인 상태에서 사용자가 로컬 파일을 지웠을 때 `edit` 이
  우연히 복구해 주던 경로가 사라집니다. 설계된 복구가 아니라 부수 효과였으며, 정식
  복구는 향후 reconcile 기능의 몫입니다.

---

## [0.3.5] — 2026-09-10

### 추가
- **설치된 버전을 화면에 표시.** 플러그인 이름, 카테고리 탭, 페이지 제목에 버전이
  붙습니다. 하드코딩이 아니라 `VERSION` 파일을 읽으므로 표시되는 값이 곧 설치본입니다.
  (탭 표기는 0.3.7 에서 제거되었습니다.)

---

## [0.3.4] — 2026-09-10

### 추가
- **복사가 끝나면 그 책 폴더만 자동으로 증분 스캔.** 파일이 로컬에 완전히 내려앉으면
  그 파일이 속한 **책 폴더 하나**를 BookOasis 스캔에 넣어 새로 들어온 권만 등록합니다.

  본체의 `scan_library_path(..., force=False)` 에 위임합니다. 라이브러리 전체를 훑지
  않으므로 파일 하나 때문에 수만 개 파일을 다시 검사하지 않습니다. 이미 등록된 권은
  본체의 mtime 스킵으로 건너뜁니다.

  `SCAN_DEBOUNCE_SECONDS`(기본 60초) 동안 그 폴더에 새 파일이 들어오지 않으면
  스캔합니다. 시리즈 여러 권이 나뉘어 도착해도 스캔은 한 번으로 접힙니다.

- 목록 화면의 경로 칸에 스캔 상태가 한 줄로 표시됩니다
  (대기 / 진행 중 / 완료 · 새 도서 n권 / 실패 / 건너뜀).

- 설정에 `AUTO_SCAN`(기본 켜짐), `SCAN_DEBOUNCE_SECONDS` 추가.

### 참고
- 스캔은 전용 스레드에서 돕니다. 본체 스캔 함수가 동기이고 수십 초가 걸릴 수 있어,
  폴링 스레드에서 부르면 그동안 감지·복사가 멈추기 때문입니다.
- 라이브러리 루트 바로 아래에 떨어진 파일과, 대응하는 보관함이 없는 폴더는
  스캔하지 않습니다(각각 `library_root`, `no_library` 로 기록).
- 프로세스가 스캔 도중 종료되면 다음 기동 때 그 항목을 대기 상태로 되돌립니다.
- 스캔 이력은 작업 이력과 같은 보존 기간 설정을 따라 정리됩니다.

### 이전 판(0.3.3)과의 차이
0.3.3 은 라이브러리 **전체** 스캔을 큐에 넣었습니다. 파일 하나가 내려올 때마다
라이브러리 전체 트리 탐색·DB 대조·무결성 점검이 함께 돌아 비용이 컸습니다.
0.3.4 는 폴더 단위로 바꾸고 큐를 거치지 않습니다.

---

## [0.3.3] 이전

`STATUS.md`(비공개 인수인계 문서)와 커밋 이력을 참고하십시오.
0.3.2 에서 플러그인 게시판 설치 검증 통과, 0.3.1 에서 자동 업데이트 활성화가
이루어졌습니다.

---

[0.3.11]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.11
[0.3.10]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.10
[0.3.9]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.9
[0.3.8]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.8
[0.3.7]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.7
[0.3.6]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.6
[0.3.5]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.5
[0.3.4]: https://github.com/scoop99/gdrive_reading_sync/releases/tag/v0.3.4
